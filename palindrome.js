// Task:
// 1. Remove any non-alphanumeric characters from the string.
// 2. Ignore casing.
// 3. Check if the string is a palindrome.

function isPalindrome(str) {
    return false;
}
  
  console.log("true -> " + isPalindrome("A man, a plan, a canal: Panama")); // true
  console.log("true -> " + isPalindrome("racecar")); // true
  console.log("false -> " + isPalindrome("hello")); // false